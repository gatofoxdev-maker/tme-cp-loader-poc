# CP Loader Injection Payload

This repository hosts `customerportal.js` for the `www.lexus.fi` CP loader injection PoC.

Use base URL (without `.js`):

`https://cdn.jsdelivr.net/gh/gatofoxdev-maker/tme-cp-loader-poc@main/customerportal`
